// import { fromJS } from 'immutable';
// import { makeSelectAllDrugsDomain } from '../selectors';

// const selector = makeSelectAllDrugsDomain();

describe('makeSelectAllDrugsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false)
  })
})
