// import { fromJS } from 'immutable';
// import { makeSelectEditDrugPageDomain } from '../selectors';

// const selector = makeSelectEditDrugPageDomain();

describe('makeSelectEditDrugPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false)
  })
})
