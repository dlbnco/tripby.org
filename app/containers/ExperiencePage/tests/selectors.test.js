// import { fromJS } from 'immutable';
// import { makeSelectExperiencePageDomain } from '../selectors';

// const selector = makeSelectExperiencePageDomain();

describe('makeSelectExperiencePageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false)
  })
})
