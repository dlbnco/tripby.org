// import { fromJS } from 'immutable';
// import { makeSelectDrugPageDomain } from '../selectors';

// const selector = makeSelectDrugPageDomain();

describe('makeSelectDrugPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
